import { TeclaPressionadaDirective } from './tecla-pressionada.directive';

describe('TeclaPressionadaDirective', () => {
  it('should create an instance', () => {
    const directive = new TeclaPressionadaDirective();
    expect(directive).toBeTruthy();
  });
});
